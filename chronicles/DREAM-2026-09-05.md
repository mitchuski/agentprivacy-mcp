# Dream — agentprivacy-mcp — 2026-09-05

*Per-directory dream (the guide's dream-cycle template). Compresses to the master chronicle
`agentprivacy_master/docs/chronicles/2026-09-05_the-agent-walks-the-guide.md` (unsigned).*

## What this is

The home of the Verifiable Trust Agent lane: the Mage's read tools over the guide's bake
(`server.mjs`), the Swordsman's write tools in a second process (`swordsman/`), the public
verify + did:key module (`lib/sign.mjs`), the κ primitive (`lib/kappa.mjs`), the lattice canon
(`lib/lattice.mjs`), the PNG carrier (`lib/png.mjs`), the scripted agent (`bin/walk-demo.mjs`),
four test suites, the handoff note for mages.city (`docs/WEAVE_mages-city.md`). Zero
dependencies. Not a git repository yet.

## Current state & open threads (within the dir)

- Rung 1 built and held (swordsman 18/18, demo `--sign` 13/13, canon 24/24, bridge 8/8, SDK
  interop ok). No Swordsman identity exists on this machine — only throwaway keystores in tests.
- `compare_plain` is development-only and must retire at Rung 3.
- `guide_search` returns fewer than five steps for some queries by design (per-site cap + score
  floor); the demo tops up by neighbours.
- `vta_publish` without a key republishes the ledger head verbatim (the ledger stores whole
  records; it holds no secret).
- `card_verify` uses the ceremony's exact message shape (plain `JSON.stringify` in field order,
  NOT the sorted canon) — a second canonical rule in the suite, inherited, not chosen.

## Guide coverage (projected vs missing)

| guide site | item | status | source path |
|---|---|---|---|
| harness | the Verifiable Trust Agent (the split made literal) | BUILT 09-05 (builder `vta-lane`) | `docs/PLAN_KNOWLEDGE_GRAPH_TO_VTA_2026-09-03.md` §4 |
| harness | the Swordsman process + policy | BUILT 09-05 | `swordsman/swordsman.mjs`, `swordsman/policy.json` |
| harness | the VTA record + what a VTA publishes | BUILT 09-05 | `lib/sign.mjs`, `docs/WEAVE_mages-city.md` |
| harness | the Mage's tools | BUILT 09-05 | `README.md` tool table |
| guide | page posture (how to state a stance) | BUILT 09-05 | `agentprivacy.guide/tools/posture.mjs` |
| guide | the walk as content (key.evolve) | BUILT 09-05 | `lib/key.mjs` |
| dtg | the Rungs (crypto plan) + `evolved_since(t)` | BUILT 09-05 | `docs/PLAN_CITY_KEY_CRYPTO_UPGRADE_2026-09-03.md` |
| dtg | the predicate atlas | MISSING (Phase 2) | — |
| skill | an `agentprivacy-vta-mage` skill packet | MISSING | — |

## Suggestions (prioritized)

| suggestion | target | why | priority |
|---|---|---|---|
| Render the `evolved_since(t)` atlas panel on /star (Phase 2, first predicate) | soulbis /star · dtg | timed for the Task Force strawman; the system can already prove it | H |
| Rung 2 sealed carriers (X25519 carrier key · HKDF · AES-GCM) | `swordsman/` | days; makes the sigil private to a recipient | H |
| Posture tomes / city / atlas with the keeper's rulings | `~/.wiki` via `posture.mjs` | the drift audit is silent until then | M |
| Wire spellweb inscribe/keepsake through the bridge | `~/spellweb` | Phase 1's UI half | M |
| A `wiki-plugin-posture` renderer | `~/skill sync/plugin/` pattern | the posture reads as a stance, not a label | L |
| Mint a skill packet for the lane | Skill Sync | so other agents can adopt the walk | L |

## To get rolling tomorrow (ordered next actions)

1. `npm test` here — everything must still hold before anything new.
2. Decide the Swordsman's home (this machine vs a second device); if here, `init --seed` with the ceremony key.
3. Read `chronicles/DREAM-LOG.md` (the deterministic census `bin/dream.mjs` appends) for what moved outside this dir.
4. Pick from the suggestions above; the atlas first if the strawman date stands.
5. (15:00 tick) The board's `docs/AGENTIC_VTI.md` names two keys per agent — the AgentCard
   (ours) and a gate-provisioned `did:webvh` persona in the cloud VTA. Decide with the other
   window how the card ↔ persona bridge is stated (a signed statement each way is proposed in
   `docs/WEAVE_mages-city.md`); do not add a `persona` field to the record until then.
6. (16:00 tick) The board's Exchange (`docs/EXCHANGE.md`) wants packet bodies sealed VTA to
   VTA and VRCs that cite a terms digest. Rung 2's `carrier.seal` should take a packet body as
   well as a sigil chunk; Rung 5's commitment shape should be `{intersection, termsDigest}`.
   Both noted in `docs/WEAVE_mages-city.md`.
7. (16:30 tick) The board's Bridges export to **Skill Sync's** sky, not the guide's lattice
   chart — two skies. Decide later whether Exchange packet pages carry posture items or the
   guide chart reads `star.json` as a remote site. The City Key's `packets` digest is the
   natural commitment to a resident's Exchange packets; carry it in `vta_publish` at Rung 5.
8. (evening tick) The board's `CITY_KEY_LOOP.md` reads `walks[]` as we defined it and built its
   own verifier (`gate/citykey.mjs`) on the same recipes. Propose one shared `conformance.json`
   (κ · Merkle · element · a signed record) in `cityofmages/mages-city/` for both test suites;
   tell the gate to verify the VTA record rather than mint a signature-over-the-key. The three
   proposed additive fields (`relationships` · `receipts` · `credentials`) are one ruling.
9. (night tick) The Zero spellbook revamp reached the sky: the farm's Zero site gained
   `zero-tale-32` and the re-bake seated it (2,218 pages). It sits on the spellbooks strand
   because it carries no posture — a tale page's natural posture is its blade's vertex (the
   Flock: V47); consider having the library builder write the tale's vertex as its posture.
10. (23:20 tick) The board's front is LIVE at mages.city (D1); the board follows behind the
    tunnel; the City repo's `deploy/` + `DECISIONS` are the deployment truth now (D11). The
    discovery map carries a `launch` block; update its `hosts` when the three zones
    (mages.city · mages.world · mages.earth) are ruled, and flip door statuses to `live` as
    `wiki.` and `portal.` answer. The reflect doors ship with the launch post.

## Notes & uncertainties

- "VTA" as a term may collide with OpenVTC's vocabulary (plan §8 Q1) — match before anything publishes.
- The visual check of the re-baked star chart has not run (browser extension disconnected).
- Nothing here is pushed or committed; publication is the First Person's act.
